% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aux_functions.R
\name{as_capacity_function}
\alias{as_capacity_function}
\title{Coerce a testing-capacity input to a capacity-generating \link{function}.}
\usage{
as_capacity_function(test_capacity)
}
\arguments{
\item{test_capacity}{An \R object: passed through from the
\code{test_capacity} argument of \code{\link[=intervention_opts]{intervention_opts()}}.}
}
\value{
A \verb{function(day_seq, N)}.
}
\description{
Coerce a testing-capacity input to a capacity-generating \link{function}.
}
\details{
Used by \code{\link[=intervention_opts]{intervention_opts()}} so that users can supply daily test
capacity as a constant \code{numeric} scalar, a \code{function} of time (\code{t}: days
since the exposure of the initial cases on day 0), or a \code{function} of
current outbreak size (\code{N}: cumulative cases so far). All three are
coerced into one canonical form -- a \verb{function(day_seq, N)} returning a
non-negative capacity (\code{integer} or \code{Inf}) for each day in \code{day_seq} --
so the rest of the simulation never needs to know which form the user
supplied.

A \code{t}-function must be vectorised: it is called once per generation with
the full \code{day_seq} (matching the calling convention of
\code{\link[=as_prob_function]{as_prob_function()}}, used for \code{test_sensitivity}/\code{symptomatic_traced}),
not once per day, so e.g. \verb{\\(t) ifelse(t < 30, 0, 500)} rather than
\verb{\\(t) if (t < 30) 0 else 500}. This is checked at construction time (like
\code{\link[=as_prob_function]{as_prob_function()}}) so a non-vectorised function fails immediately in
\code{\link[=intervention_opts]{intervention_opts()}}, rather than deep inside a simulation. An
\code{N}-function is evaluated once at the current outbreak size and that
single value is recycled across \code{day_seq} (capacity is constant across
the days being queried, but can change between calls as the outbreak
grows) -- vectorisation doesn't apply here, since it's always called
with a scalar \code{N}.
}
\keyword{internal}
