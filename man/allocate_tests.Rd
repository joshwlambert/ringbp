% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_testing.R
\name{allocate_tests}
\alias{allocate_tests}
\title{Allocate a fixed daily test quota across candidates competing for tests
on the same day}
\usage{
allocate_tests(day, test_quota)
}
\arguments{
\item{day}{an \code{integer} vector: the day each candidate would be tested
on, one element per candidate.}

\item{test_quota}{a \code{data.table} with columns \code{day} and
\code{tests_remaining}: the available daily quota. \emph{\strong{Note}} modified by
reference to debit the tests allocated, see \code{\link[data.table:set]{data.table::set()}}.}
}
\value{
A \code{logical} vector the same length as \code{day}, \code{TRUE} iff that
candidate was allocated a test.
}
\description{
Shared by \code{\link[=outbreak_setup]{outbreak_setup()}} (index cases) and \code{\link[=sample_testing]{sample_testing()}}
(later generations): whenever a set of candidates each need a test on
a given day, and the day's capacity may not stretch to all of them,
this function decides who gets one.
}
\keyword{internal}
