% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outbreak_step.R
\name{outbreak_step}
\alias{outbreak_step}
\title{Move forward one generation in the branching process}
\usage{
outbreak_step(case_data, offspring, delays, event_probs, interventions)
}
\arguments{
\item{case_data}{a \code{data.table}: cases in outbreak so far; initially
generated by \code{\link[=outbreak_setup]{outbreak_setup()}}}

\item{offspring}{a \code{list} with class \verb{<ringbp_offspring_opts>}: the
offspring distribution \code{function}s for the \pkg{ringbp} model, returned
by \code{\link[=offspring_opts]{offspring_opts()}}. Contains six elements: \code{community}, \code{isolated},
\code{asymptomatic}, and the three \verb{*_contact_prob_infect} probabilities}

\item{delays}{a \code{list} with class \verb{<ringbp_delay_opts>}: the
delay distribution \code{function}s for the \pkg{ringbp} model, returned
by \code{\link[=delay_opts]{delay_opts()}}. Contains 4 elements: \code{incubation_period},
\code{onset_to_isolation}, \code{latent_period} and \code{onset_to_self_isolation}}

\item{event_probs}{a \code{list} with class \verb{<ringbp_event_prob_opts>}: the
event probabilities for the \pkg{ringbp} model, returned by
\code{\link[=event_prob_opts]{event_prob_opts()}}. Contains 5 elements: \code{asymptomatic},
\code{presymptomatic_transmission}, \code{alpha}, \code{symptomatic_traced} and
\code{symptomatic_self_isolate}}

\item{interventions}{a \code{list} with class \verb{<ringbp_intervention_opts>}:
the intervention settings for the \pkg{ringbp} model, returned by
\code{\link[=intervention_opts]{intervention_opts()}}. Contains 3 elements: \code{quarantine},
\code{test_sensitivity} and \code{test_capacity}. May also carry a \code{test_quota}
element (a \code{data.table} of remaining capacity by day, carried over from
a previous call) -- if absent, every day starts at full capacity.}
}
\value{
A \code{list} with four elements:
\enumerate{
\item \verb{$cases}: a \code{data.table} with case data
\item \verb{$effective_r0}: a \code{numeric} with the effective reproduction number
\item \verb{$cases_in_gen}: a \code{numeric} with the number of new cases in that
generation
\item \verb{$test_quota}: a \code{data.table} (\code{day}, \code{tests_remaining}) with the
remaining test capacity by day, to pass to the next call as
\code{interventions$test_quota}
}
}
\description{
Move forward one generation in the branching process
}
\details{
Each new case is assigned an isolation time (\code{isolated_time}) as the
earliest of up to three pathways; a case to which no pathway applies is
never isolated (\code{isolated_time} is \code{Inf}):
\itemize{
\item \strong{Self-isolation}: a symptomatic case self-isolates with probability
\code{symptomatic_self_isolate} (from \code{\link[=event_prob_opts]{event_prob_opts()}}), entering isolation
an \code{onset_to_self_isolation} delay (from \code{\link[=delay_opts]{delay_opts()}}) after symptom
onset. Self-isolating cases are not tested.
\item \strong{Testing}: a symptomatic case that does not self-isolate, and is not
already destined for exposure-based quarantine (see \strong{Tracing} below),
is a candidate for testing, entering isolation an \code{onset_to_isolation}
delay after symptom onset if it is (a) allocated one of the day's
tests, subject to \code{test_capacity} (from \code{\link[=intervention_opts]{intervention_opts()}}, shared
with uninfected traced contacts -- see \code{\link[=sample_testing]{sample_testing()}}), and (b)
that test returns a positive result, with probability
\code{test_sensitivity} (from \code{\link[=intervention_opts]{intervention_opts()}}). Neither a missed
allocation nor a false-negative result isolates the case via this
pathway. A case already destined for quarantine is excluded from
testing entirely, since quarantine isolates it independent of the test
result -- it does not compete for capacity that gates other
individuals' isolation.
\item \strong{Tracing}: a case whose infector is symptomatic is traced with
probability \code{symptomatic_traced} (from \code{\link[=event_prob_opts]{event_prob_opts()}}). When
\code{quarantine} is active (from \code{\link[=intervention_opts]{intervention_opts()}}) tracing is
exposure-based: a traced case is isolated when its infector is isolated,
regardless of its own symptom status, and is excluded from the testing
pathway above. Without \code{quarantine}, only a traced symptomatic case is
isolated, no earlier than its own symptom onset, and remains eligible
for testing (able to be isolated earlier via a positive test, or as
confirmation alongside tracing).
}

Self-isolation and testing are symptom-based, so asymptomatic cases are
isolated only via the tracing pathway, and only when \code{quarantine} is active.
}
\examples{
offspring <- offspring_opts(
  community = \(n) rnbinom(n = n, mu = 2.5, size = 0.16),
  isolated = \(n) rnbinom(n = n, mu = 0, size = 1),
  asymptomatic = \(n) rnbinom(n = n, mu = 1.25, size = 0.16)
)
delays <- delay_opts(
  incubation_period = \(n) rweibull(n = n, shape = 2.32, scale = 6.49),
  onset_to_isolation = \(n) rweibull(n = n, shape = 1.65, scale = 4.28)
)
event_probs <- event_prob_opts(
  asymptomatic = 0,
  presymptomatic_transmission = 0.15,
  symptomatic_traced = 0
)
interventions <- intervention_opts(quarantine = FALSE)

# generate initial cases
case_data <- outbreak_setup(
  initial_cases = 5,
  delays = delays,
  event_probs = event_probs,
  interventions = interventions
)
case_data
# carry the test quota that outbreak_setup() already allocated against
interventions$test_quota <- attr(case_data, "test_quota")
# generate next generation of cases
out <- outbreak_step(
  case_data = case_data,
  offspring = offspring,
  delays = delays,
  event_probs = event_probs,
  interventions = interventions
)
case_data <- out$cases
case_data
}
