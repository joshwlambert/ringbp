% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aux_functions.R
\name{control}
\alias{control}
\alias{control_prob}
\alias{extinct_prob}
\alias{detect_control}
\alias{detect_extinct}
\title{Outbreak control and extinction functions}
\usage{
control_prob(scenario, control_week = NULL, control_threshold = 0)

extinct_prob(scenario, extinction_week = NULL)

detect_control(scenario, control_week = NULL, control_threshold = 0)

detect_extinct(scenario, extinction_week = NULL)
}
\arguments{
\item{scenario}{a \code{data.table}: weekly cases output by \code{\link[=scenario_sim]{scenario_sim()}}}

\item{control_week, extinction_week}{an \code{integer} scalar, \code{integer} vector,
or \code{NULL} (default): the week (zero-indexed) or set of weeks over which
to test whether the outbreak has been controlled / went extinct.

\code{control_week} is used by \code{\link[=control_prob]{control_prob()}} and \code{\link[=detect_control]{detect_control()}};
\code{extinction_week} is used by \code{\link[=extinct_prob]{extinct_prob()}} and \code{\link[=detect_extinct]{detect_extinct()}}.
They behave identically and differ only in name and intent:
\code{control_week} is paired with \code{control_threshold} and tests whether
\code{weekly_cases <= control_threshold} across the specified weeks;
\code{extinction_week} is the same test fixed to \code{control_threshold = 0},
i.e. \code{weekly_cases == 0} (extinction). Permitted forms:
\itemize{
\item \code{NULL} (default): use the pre-computed extinction status (\code{extinct}
attribute) attached to the output of \code{\link[=scenario_sim]{scenario_sim()}} — a
true-extinction flag set when all infectious cases had the opportunity
to transmit but no new cases were generated. Only valid when
\code{control_threshold = 0}; when \code{control_threshold > 0} an explicit
\code{control_week} is required and the default will error.
\item A single \code{integer}, e.g. \code{5}: tests the window from week \code{5} through
the last simulated week. For example, \code{extinction_week = 5} tests
whether no cases occur from week 5 onwards; \code{control_week = 5} with
\code{control_threshold = 10} tests whether weekly cases stay at most 10
from week 5 onwards.
\item An \code{integer} vector of length two, e.g. \code{c(5, 10)}: gives the
lower and upper bounds (inclusive) of the week range to test. For
example, \code{extinction_week = c(5, 10)} tests whether there were no new
cases between weeks 5 and 10 (inclusive); \code{control_week = c(5, 10)}
tests whether weekly cases stayed within \code{control_threshold} over the
same window.
\item An \code{integer} vector of length \emph{n} (n >= 2), e.g. \code{12:16}: the exact
set of weeks to test (12, 13, 14, 15, 16). For example,
\code{extinction_week = 12:16} tests for no new cases across those weeks,
and \code{control_week = 12:16} tests for weekly cases at or below
\code{control_threshold} across them. Sequences are usually contiguous but
non-contiguous integer vectors are allowed.
}

An outbreak that becomes controlled / extinct \emph{before} the start of the
window is still classified as controlled / extinct, because every week
within the window then satisfies \code{weekly_cases <= control_threshold}. An
outbreak that only becomes controlled / extinct \emph{partway through} the
window is not, because at least one in-window week exceeded the
threshold. So a single \code{integer} for \code{extinction_week} reads naturally
as "\emph{has the outbreak gone extinct by week X}".}

\item{control_threshold}{an \code{integer} scalar: the threshold number of weekly
cases to classify the outbreak as controlled. By default it is \code{0} which
is outbreak extinction. Values greater than zero correspond
to control defined under outbreak suppression strategies, e.g., if weekly
cases do not exceed 100 (\code{control_threshold = 100}), then the outbreak is
said to be under control in that period.}
}
\value{
\code{control_prob()}: a single \code{numeric} with the probability of control

\code{extinct_prob()}: a single \code{numeric} with the probability of extinction

\code{detect_control()}: a \code{data.table}, with two columns \code{sim} and \code{control},
for a binary classification of whether the outbreak was controlled in each
simulation replicate. \code{1} is an outbreak that was controlled, \code{0} if not.

\code{detect_extinct()}: a \code{data.table}, with two columns \code{sim} and \code{extinct},
for a binary classification of whether the outbreak went extinct in each
simulation replicate. \code{1} is an outbreak that went extinct, \code{0} if not.
}
\description{
\code{control_prob()}: Calculate proportion of runs that have a controlled
outbreak

\code{extinct_prob()}: Calculate proportion of runs with an extinct outbreak (a
special case of \code{control_prob()} with \code{control_threshold = 0})

\code{detect_control()}: Calculate whether outbreaks were controlled or not

\code{detect_extinct()}: Calculate whether outbreaks went extinct or not

\code{extinct_prob()} and \code{detect_extinct()} were added to the package before
\code{control_prob()} and \code{detect_control()}. The extinction functions are
special cases of the more general control functions by fixing
\code{control_threshold = 0}. They remain in the package for backwards
compatibility and convenience to calculate extinction metrics.
}
\details{
The data passed to \code{scenario} has to be produced by \code{\link[=scenario_sim]{scenario_sim()}}.
It cannot be produced by \code{\link[=outbreak_model]{outbreak_model()}} as it requires the \code{sim}
column, which is only appended in \code{\link[=scenario_sim]{scenario_sim()}}.

\emph{\strong{Warning}}: the output from \code{\link[=scenario_sim]{scenario_sim()}} contains an \code{cap_cases}
attribute which is used by \code{\link[=control_prob]{control_prob()}}, \code{\link[=extinct_prob]{extinct_prob()}},
\code{\link[=detect_control]{detect_control()}}, and \code{\link[=detect_extinct]{detect_extinct()}}, therefore if you modify the
output of \code{\link[=scenario_sim]{scenario_sim()}} before passing to any of the \link{control}
functions be careful not to drop the attribute (e.g. from subsetting the
\code{data.table}).
}
\examples{
res <- scenario_sim(
  n = 10,
  initial_cases = 1,
  offspring = offspring_opts(
    community = \(n) rnbinom(n = n, mu = 2.5, size = 0.16),
    isolated = \(n) rnbinom(n = n, mu = 0.5, size = 1)
  ),
  delays = delay_opts(
    incubation_period = \(n) rweibull(n = n, shape = 2.32, scale = 6.49),
    onset_to_isolation = \(n) rweibull(n = n, shape = 1.65, scale = 4.28)
  ),
  event_probs = event_prob_opts(
    asymptomatic = 0,
    presymptomatic_transmission = 0.5,
    symptomatic_ascertained = 0.2
  ),
  interventions = intervention_opts(quarantine = FALSE),
  sim = sim_opts(cap_max_days = 350, cap_cases = 4500)
)

# calculate probability of extinction
extinct_prob(res)

# determine if each outbreak simulation replicate has gone extinct
detect_extinct(res)

# calculate extinction in the last 2 weeks of the simulated outbreak
# (i.e. the penultimate and last week of the outbreak)
extinct_prob(res, extinction_week = max(res$week) - 1)

# calculate extinction as no new cases between weeks 12 and 16 of the outbreak
extinct_prob(res, extinction_week = 12:16)

# calculate probability of control as weekly cases at or below 5
# between weeks 12 and 16 of the outbreak
control_prob(res, control_week = 12:16, control_threshold = 5)

# determine for each simulation whether weekly cases stayed at or below 5
# between weeks 12 and 16
detect_control(res, control_week = 12:16, control_threshold = 5)

# calculate probability of control as weekly cases at or below 10
# from week 20 to the end of the outbreak
control_prob(res, control_week = 20, control_threshold = 10)
}
